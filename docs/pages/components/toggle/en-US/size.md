### Size

The `size` property sets the switch size, and the value includes: `lg`、`md`、`sm`.

<!--start-code-->

```js
const instance = (
  <div>
    <Toggle size="lg" />
    <Toggle size="md" />
    <Toggle size="sm" />
  </div>
);
ReactDOM.render(instance);
```

<!--end-code-->
