<!--start-code-->

```js
import { Image } from 'rsuite';

const App = () => {
  return (
    <Image
      zoomed
      src="https://images.unsplash.com/photo-1518374835801-2c7d993de05c?w=256"
      alt="person near brown puppy in gray camera bag with camera kit inside"
      width={160}
    />
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
```

<!--end-code-->
