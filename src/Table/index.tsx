import Table from './Table';
import type { TableProps, ColumnProps, ColumnGroupProps, TableLocaleType } from 'rsuite-table';
export type { TableProps, ColumnGroupProps, ColumnProps, TableLocaleType };
export type { TableInstance, CellProps } from './Table';

export default Table;
